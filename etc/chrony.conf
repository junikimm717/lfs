# Use public NTP servers
server pool.ntp.org iburst

# Record drift for hardware clock
driftfile /var/lib/chrony/chrony.drift

# Optional logging (can comment out in embedded systems)
logdir /var/log/chrony

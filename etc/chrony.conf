server time.google.com iburst
pool pool.ntp.org iburst
driftfile /var/lib/chrony/drift
makestep 1.0 -1
logdir /var/log/chrony

# Keeps the clock usable with no reachable server; real servers still win.
local stratum 10
